
#include "utils/Params.h"

/**
 * @brief Parse XML file and read parameters, which need to be read in
 * before instantiation of the simulation
 * @param params
 * @param filename
 */
void xmlparse(Params& params, std::string& filename);
